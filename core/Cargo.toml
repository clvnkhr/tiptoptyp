[package]
name = "tiptoptyp-core"
version = "0.1.0"
edition = "2024"
publish = false
rust-version = "1.98"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[features]
test-support = []

[[bench]]
name = "document"
harness = false
