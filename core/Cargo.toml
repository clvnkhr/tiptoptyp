[package]
name = "tiptoptyp-core"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[features]
test-support = []

[[bench]]
name = "document"
harness = false
